<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fun Portal</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: url('https://images.unsplash.com/photo-1521737604893-d14cc237f11d?auto=format&fit=crop&w=1950&q=80') no-repeat center center fixed;
      background-size: cover;
      color: white;
      margin: 0;
      padding: 0;
    }
    header {
      background-color: rgba(0,0,0,0.6);
      text-align: center;
      padding: 20px;
      font-size: 2em;
    }
    main {
      max-width: 800px;
      margin: 30px auto;
      padding: 20px;
      background-color: rgba(0,0,0,0.7);
      border-radius: 10px;
    }
    a {
      display: inline-block;
      margin: 10px 0;
      color: #FFD700;
      text-decoration: none;
      font-weight: bold;
    }
    a:hover {
      color: #FF6347;
    }
    button {
      padding: 10px 20px;
      font-size: 1em;
      margin-top: 10px;
      border-radius: 5px;
      border: none;
      cursor: pointer;
      background-color: #4CAF50;
      color: white;
    }
    button:hover {
      background-color: #45a049;
    }
    #minesweeper {
      margin-top: 20px;
    }
    table {
      border-collapse: collapse;
      margin-top: 10px;
    }
    td {
      width: 30px;
      height: 30px;
      text-align: center;
      vertical-align: middle;
      border: 1px solid #555;
      cursor: pointer;
      background-color: #888;
      font-weight: bold;
      color: black;
    }
    td.revealed {
      background-color: #ccc;
    }
  </style>
</head>
<body>

<audio autoplay loop>
  <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
</audio>

<header>🎮 Fun Portal 🎮</header>

<main>
  <h2>Links to Cool Math Games</h2>
  <a href="https://www.coolmathgames.com/" target="_blank">Cool Math Games</a><br>
  <a href="https://www.mathplayground.com/" target="_blank">Math Playground</a>

  <h2>Soundtrap</h2>
  <a href="https://www.soundtrap.com/" target="_blank">Go to Soundtrap</a>

  <h2>Minesweeper</h2>
  <div id="minesweeper"></div>

</main>

<script>
// --- Minesweeper ---
const ROWS = 5;
const COLS = 5;
const MINES = 5;
let board = [];
let revealed = [];

function initMinesweeper() {
  const container = document.getElementById("minesweeper");
  container.innerHTML = "";

  // create arrays
  board = Array(ROWS).fill().map(() => Array(COLS).fill(0));
  revealed = Array(ROWS).fill().map(() => Array(COLS).fill(false));

  // place mines
  let minesPlaced = 0;
  while (minesPlaced < MINES) {
    let r = Math.floor(Math.random() * ROWS);
    let c = Math.floor(Math.random() * COLS);
    if (board[r][c] !== "M") {
      board[r][c] = "M";
      minesPlaced++;
    }
  }

  // calculate numbers
  for (let r = 0; r < ROWS; r++) {
    for (let c = 0; c < COLS; c++) {
      if (board[r][c] === "M") continue;
      let count = 0;
      for (let dr=-1; dr<=1; dr++) {
        for (let dc=-1; dc<=1; dc++) {
          let nr = r + dr;
          let nc = c + dc;
          if (nr>=0 && nr<ROWS && nc>=0 && nc<COLS && board[nr][nc]==="M") count++;
        }
      }
      board[r][c] = count;
    }
  }

  // create table
  const table = document.createElement("table");
  for (let r = 0; r < ROWS; r++) {
    const tr = document.createElement("tr");
    for (let c = 0; c < COLS; c++) {
      const td = document.createElement("td");
      td.dataset.row = r;
      td.dataset.col = c;
      td.addEventListener("click", revealCell);
      tr.appendChild(td);
    }
    table.appendChild(tr);
  }
  container.appendChild(table);
}

function revealCell(e) {
  const r = parseInt(this.dataset.row);
  const c = parseInt(this.dataset.col);
  if (revealed[r][c]) return;

  revealed[r][c] = true;
  this.classList.add("revealed");

  if (board[r][c] === "M") {
    this.innerText = "💣";
    alert("Game Over!");
    initMinesweeper();
  } else if (board[r][c] === 0) {
    this.innerText = "";
    // reveal neighbors recursively
    for (let dr=-1; dr<=1; dr++) {
      for (let dc=-1; dc<=1; dc++) {
        let nr = r + dr;
        let nc = c + dc;
        if (nr>=0 && nr<ROWS && nc>=0 && nc<COLS) {
          const td = document.querySelector(`td[data-row='${nr}'][data-col='${nc}']`);
          if (td && !revealed[nr][nc]) td.click();
        }
      }
    }
  } else {
    this.innerText = board[r][c];
  }
}

// initialize on load
initMinesweeper();
</script>

</body>
</html>
